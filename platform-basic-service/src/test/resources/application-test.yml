spring:
  datasource:
    url: jdbc:h2:mem:energy_platform_basic;MODE=MySQL;DATABASE_TO_UPPER=false;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true

app:
  log-service:
    base-url: http://127.0.0.1:1
